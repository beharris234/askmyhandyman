{
  "name": "tenderism-app",
  "version": "1.0.0",
  "private": true,
  "description": "Tenderism — fall-off-the-bone meat cooking guide. Native wrapper (Capacitor).",
  "scripts": {
    "copy:web": "node ./scripts/sync-web.js",
    "sync": "npm run copy:web && npx cap sync",
    "add:ios": "npx cap add ios",
    "add:android": "npx cap add android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "run:ios": "npm run sync && npx cap run ios",
    "run:android": "npm run sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/local-notifications": "^6.1.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
